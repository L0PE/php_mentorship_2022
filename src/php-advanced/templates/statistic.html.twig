<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Text information statistic{% endblock %}</title>
    <link rel="icon"
          href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body>
<div class="container">
    <div class="py-5 text-center">
        <h2>Text information</h2>
        <p class="lead">Global Statistic</p>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                <div>
                    <a class="me-3 btn btn-outline-success" href="{{ path('home') }}">Home</a>
                    <a class="me-3 btn btn-outline-success" href="{{ path('global_statistic') }}">Global Statistic</a>
                </div>
            </div>
        </nav>
    </div>
    <form method="post" enctype="multipart/form-data" action="">
        <div class="input-group mb-3">
            <span class="input-group-text" id="inputGroup-sizing-default">Start Date</span>
            <input type="date" name="start_date" class="form-control" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-default">
        </div>
        <div class="input-group mb-3">
            <span class="input-group-text" id="inputGroup-sizing-default">End Date</span>
            <input type="date" name="end_date" class="form-control" aria-label="Sizing example input" aria-describedby="inputGroup-sizing-default">
        </div>
        <button id="submit" name="submit" type="submit" class="btn btn-success">Submit</button>
    </form>
    <div class="row mt-5">
        <h2 class="text-center">Statistic</h2>
        <dl class="row">
            <dt class="col-sm-5">Number of text:</dt>
            <dd class="col-sm-7">{{ statistic.1 }}</dd>

            <dt class="col-sm-5">Number of characters:</dt>
            <dd class="col-sm-7">{{ statistic.2|round(1) }}</dd>

            <dt class="col-sm-5">Number of words:</dt>
            <dd class="col-sm-7">{{ statistic.3|round(1) }}</dd>

            <dt class="col-sm-5">Number of sentences:</dt>
            <dd class="col-sm-7">{{ statistic.4|round(1) }}</dd>

            <dt class="col-sm-5">Average Word Length:</dt>
            <dd class="col-sm-7">{{ statistic.5|round(1) }}</dd>

            <dt class="col-sm-5">The average number of words in a sentence:</dt>
            <dd class="col-sm-7">{{ statistic.6|round(1) }}</dd>

            <dt class="col-sm-5">Number of palindrome words:</dt>
            <dd class="col-sm-7">{{ statistic.7|round(1) }}</dd>

            <dt class="col-sm-5">The time it took to process the text:</dt>
            <dd class="col-sm-7">{{ statistic.8|round(1) }}</dd>

        </dl>
    </div>
    </div>
</body>
</html>
